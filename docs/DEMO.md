# Demo

The public release does not include a shared writable demo or real financial data. A future hosted demo must use an isolated synthetic database and either server-enforced read-only mode or a deterministic reset process.

When preparing a demo, seed only synthetic accounts, transactions, obligations, recurring payments, liabilities and forecasts. Never connect a demo to a private deployment or reuse production credentials.
