packages:
  - apps/*
  - packages/*

# pnpm 11 blocks dependency lifecycle scripts unless explicitly reviewed.
# Keep this map narrow; additions are supply-chain-sensitive changes.
allowBuilds:
  "@parcel/watcher": true
  "@swc/core": true
  esbuild: true
